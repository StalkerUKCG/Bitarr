version: '3.8'

services:
  bitarr:
    build: .
    container_name: bitarr
    ports:
      - "5054:80"
    volumes:
      - ./config:/config
      - /path/to/your/games:/mnt/games
    restart: unless-stopped
    networks:
      - bitarr_network

networks:
  bitarr_network:
    driver: bridge